name: Manual trigger
run-name: Build ImGui_GM for Windows
on:
  workflow_dispatch:

jobs:
    build:
        runs-on: windows-latest
        steps:
        - name: Hello Step
          run: echo "Hello"
